import type { FunctionComponent, ReactElement } from 'react';

export const InstructionTest: FunctionComponent = (): ReactElement => (
  <div>
    <p>Open up App.js to start working on your app!</p>
  </div>
);
